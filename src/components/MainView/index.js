import React from "react";
import classes from "./mainView.module.css";

const MainView = () => {
  return (
    <div className={classes.mainViewContainer}>
      <div className={classes.mainViewWrapper}>MainView</div>
    </div>
  );
};

export default MainView;
